({
	myAction : function(component, event, helper) {
		
	},
    doInit: function(cmp) {
        // Set the attribute value. 
        // You could also fire an event here instead.
        
        alert(cmp.get("v.recordId") + "Mayank");
    }
})